{
  "name": "passwort-generator",
  "version": "0.0.0",
  "description": "Generates passwords from a word ranking list",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "ts-node index.ts",
    "build": "tsc",
    "start": "NODE_ENV=production node build/index.js",
    "profile": "tsc && cross-env NODE_ENV=production node --prof build/index.js"
  },
  "author": "Christoph Müller <iblzm@hotmail.de>",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "cross-env": "^7.0.0",
    "ts-node": "^8.0.0",
    "tslint": "^6.0.0",
    "typescript": "^3.8.0"
  },
  "dependencies": {
    "@types/node": "^13.0.0",
    "@types/pify": "^3.0.2",
    "@types/seedrandom": "^2.4.27",
    "pify": "^5.0.0",
    "seedrandom": "^3.0.0"
  }
}
